# ניתוח פער הצבעות — כנסת 25 לעומת אתר הכנסת הרשמי

**תאריך**: 20 באפריל 2026  
**כלי**: `src/scripts/diagnose-votes.ts`

---

## תקציר ממצאים

| מקור                                  | ספירת הצבעות (כנסת 25) |
| ------------------------------------- | ---------------------- |
| אתר הכנסת הרשמי (עם פילטר "הצעת חוק") | **6,264**              |
| האפליקציה (פילטר `activityType=bill`) | **6,198**              |
| **פער**                               | **66 הצבעות**          |

---

## 1. מצב נתוני הצבעות ב-DB

### סה"כ הצבעות לפי כנסת

| כנסת | סה"כ      |
| ---- | --------- |
| 25   | **6,836** |
| 24   | 372       |
| 23   | 1,672     |
| 22   | 68        |
| 21   | 16        |
| 20   | 7,514     |

### כנסת 25 — קישור ל-bill_id

| סטטוס                             | כמות      |
| --------------------------------- | --------- |
| קושר לחוק (`bill_id IS NOT NULL`) | **6,198** |
| לא קושר (`bill_id IS NULL`)       | **638**   |
| **סה"כ**                          | **6,836** |

> הספירה 6,198 שמוצגת באפליקציה תואמת בדיוק למספר ההצבעות המקושרות ל-`bill_id`.

---

## 2. גורם הפער — `linkVotesToBills` חסר 66 הצבעות

האפליקציה מגדירה `activityType=bill` כ-`votes.billId IS NOT NULL`.  
638 הצבעות שלא קושרו לחוק נחסרות לגמרי מהספירה.

### פירוט 638 ההצבעות הלא-מקושרות (לפי נושא כותרת)

| קטגוריה          | כמות    |
| ---------------- | ------- |
| הצעות לסדר היום  | **285** |
| הצעות אי-אמון    | **195** |
| אחר (לא מסווג)   | **97**  |
| כולל המילה "חוק" | **47**  |
| הצעת X (חקיקה)   | **14**  |

> **47 + 14 = 61 הצבעות** שכותרתן מצביעה על חקיקה, אך `linkVotesToBills` לא הצליח לקשר אותן לחוק ספציפי ב-DB.  
> ייתכן שחלק מ-97 "אחר" הן גם הצבעות חקיקה — ביחד מגיע לסדר גודל של **66**.

---

## 3. שלבי החקיקה ב-6,198 ההצבעות המקושרות

| `billStage` | תיאור                | כמות      |
| ----------- | -------------------- | --------- |
| 1           | קריאה ראשונה (הכנה)  | 124       |
| 3           | קריאה ראשונה         | 647       |
| 5           | קריאה שנייה + שלישית | **3,846** |
| 6           | קריאה שלישית נפרדת   | 191       |
| NULL        | לא זוהה שלב          | **1,390** |

---

## 4. ניתוח הסתייגויות

| `isReservation`       | כמות      |
| --------------------- | --------- |
| `false` (הצבעה רגילה) | 4,067     |
| `true` (הסתייגות)     | **2,769** |

> 40% מהצבעות כנסת 25 הן הסתייגויות.  
> הסתייגויות מוגדרות לפי מילת המפתח "הסתייגו" בכותרת.

---

## 5. שיטות הצבעה

| `voteType` | שיטה      | כמות  |
| ---------- | --------- | ----- |
| `1`        | אלקטרונית | 6,331 |
| `2`        | שמית      | 434   |
| `4`        | הצבעת יד  | 65    |
| `3`        | חשאית     | 6     |

---

## 6. כיסוי `member_votes` לכנסת 25

| סטטוס                                   | כמות      |
| --------------------------------------- | --------- |
| הצבעות עם רשומות חברים (`member_votes`) | **6,765** |
| הצבעות ללא רשומות חברים                 | **71**    |

> 71 הצבעות ללא `member_votes` הן גם הצבעות ה-zero-tally — כנראה הצבעות שה-sync שלהן לא הושלם.

---

## 7. טווח תאריכים (K25 ב-DB)

|               | תאריך      |
| ------------- | ---------- |
| הצבעה הראשונה | 15/11/2022 |
| הצבעה האחרונה | 30/03/2026 |

---

## 8. מסקנות ופעולות מומלצות

### סיבת הפער העיקרית

האפליקציה מסננת `activityType=bill` → `bill_id IS NOT NULL`.  
ישנן **66+ הצבעות חקיקה** שה-pipeline של `linkVotesToBills` לא הצליח לקשר לחוק — בעיקר בגלל שכותרות ההצבעה לא מכילות את שם החוק המדויק שב-DB.

### פעולות מומלצות

| #   | בעיה                                  | פתרון מוצע                                                                                         |
| --- | ------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 1   | 47 הצבעות עם "חוק" בכותרת לא קושרו    | הרחבת Layer 1 (substring) — matching חלקי גמיש יותר, או הורדת סף similarity ב-Layer 2 מ-0.3 ל-0.25 |
| 2   | 14 הצבעות "הצעת X" לא קושרו           | בדיקה ידנית של 14 הצבעות — ייתכן שהחוק חסר ב-DB                                                    |
| 3   | 71 הצבעות ללא `member_votes`          | הרצה חוזרת של `syncMemberVotesOnly` על VoteIDs האלה                                                |
| 4   | 1,390 הצבעות מקושרות ללא `billStage`  | הרצה חוזרת של `linkVotesToBills` Phase 5 (keyword fallback)                                        |
| 5   | פיצול לא תואם בין אתר הכנסת לאפליקציה | שקול לשנות את ההגדרה של `activityType=bill` לכלול הצבעות עם "חוק" בכותרת גם אם `billId` = null     |

---

## נספח — דוגמאות מ-20 ההצבעות האחרונות הלא-מקושרות

```
[2026-03-30] הודעת הממשלה לפי חוק הכניסה לישראל
[2026-03-30] הודעת הממשלה לפי פקודת המועצות המקומיות
[2026-03-25] הצעה דחופה לסדר היום — תיאום מחירים
[2026-03-23] הצעה להביע אי אמון בממשלה (פקרות תקציבית)
[2026-03-23] הצעה להביע אי אמון בממשלה (פקרות תקציבית) [כפולה]
[2026-03-17] הצעה דחופה לסדר היום — יישובי קו העימות
[2026-03-15] הצעה להביע אי אמון (משבר כוח האדם)
...
```

**הצבעות "הודעת ממשלה" ו"הצעות לסדר היום" ו"הצעות אי-אמון" הן תקינות** — הן לא הצבעות חקיקה ולא צריכות להיספר ב-`activityType=bill`.

---

## עדכון — 21 באפריל 2026

### תוצאות הריצה החוזרת של `linkVotesToBills`

לאחר הורדת סף ה-similarity ב-Layer 2 מ-`0.3` ל-`0.25`:

| שלב                              | קשרים חדשים |
| -------------------------------- | ----------- |
| Layer 1 (substring)              | 4           |
| Layer 2 (similarity > 0.25)      | 205         |
| Layer 3 (sessItemId propagation) | 201         |
| Layer 4 (name-history stage)     | 4           |
| Layer 5 (keyword fallback stage) | 146         |
| Layer 6 (date disambiguation)    | 0           |
| **סה"כ קשרים חדשים**             | **410**     |

- לפני ההרצה: 2,058 הצבעות לא-מקושרות (כל הכנסות).
- אחרי ההרצה: 1,648 הצבעות לא-מקושרות.
- 146 מהצבעות שכבר היו מקושרות קיבלו גם `billStage` ב-Layer 5.

### 5 הצבעות K25 הבודדות שנותרו כ"דמויות-חוק" לא-מקושרות

הסקריפט `src/scripts/list-unlinked-bill-like-votes.ts 25` מצא **5 הצבעות** בלבד (ירידה מ-14 לפני התיקון). כולן הן **false positives** של ההיוריסטיקה — לא מדובר בהצעות חוק אמיתיות אלא בהחלטות פרוצדורליות:

| #   | תאריך      | VoteID פנימי | Knesset VoteID | כותרת                                                            | סיווג אמיתי              |
| --- | ---------- | ------------ | -------------- | ---------------------------------------------------------------- | ------------------------ |
| 1   | 04/08/2025 | 18808        | 44427          | הצעת ועדת הכנסת בדבר שינוי בהרכב הסיעתי של ועדת הכלכלה           | הרכב ועדות (פרוצדורלי)   |
| 2   | 13/05/2025 | 19084        | 44054          | הצעת ועדת הכנסת בדבר שינוי בהרכב הסיעתי של ועדות הכנסת           | הרכב ועדות (פרוצדורלי)   |
| 3   | 24/02/2025 | 19853        | 43125          | הצעת ועדת הכנסת בדבר שינוי בהרכב הסיעתי של ועדות הכנסת           | הרכב ועדות (פרוצדורלי)   |
| 4   | 12/09/2024 | 20999        | 41739          | הצעת ועדת הכנסת בעניין הקמת הוועדה המיוחדת ליישובים בגבול המזרחי | הקמת ועדה (פרוצדורלי)    |
| 5   | 15/01/2023 | 24255        | 37947          | החלטת הממשלה... לצרף את חברת הכנסת מאי גולן כשרה בלי תיק...      | מינוי שרים (החלטת ממשלה) |

כל ה-5 מתחילות ב-"הצעת ועדת הכנסת..." או "החלטת הממשלה..." — אלה פעולות פרלמנטריות/פרוצדורליות שאינן חוקים. הפילטר המורחב `activityType=bill` (שכולל כותרות עם "חוק") אינו אמור להעלות אותן (הן לא מכילות את המילה "חוק"), **חוץ מהצבעה #5** שמכילה "חוק יסוד: הממשלה" ולכן תיספר כ-bill. הספירה הזו סבירה — זה אכן "חוק-יסוד".

### הצבעות ללא `member_votes`

הסקריפט `src/scripts/backfill-missing-member-votes.ts 25` הורץ למילוי 71 הצבעות שחסרו בהן רשומות חברים.

**ממצא**: 0 רשומות נוספו. הסקריפט שלף 311,866 רשומות `KNS_PlenumVoteResult` בטווח ה-VoteIDs של 71 ההצבעות (39381–45325), אך אף אחת מהרשומות לא הייתה עם VoteID התואם לאחד מ-71 ההצבעות הללו.

**מסקנה**: 71 ההצבעות קיימות ב-`KNS_PlenumVote` (ולכן יש להן הדר ב-`votes`), אך ב-`KNS_PlenumVoteResult` **אין להן שום רשומה**. זה לא כשל sync אלא מצב של הנתונים במקור — כנראה הצבעות חשאיות, הצבעות שבוטלו, או רשומות-צללים שלא נושאות תוצאה אמיתית. לא ניתן למלא אותן דרך ה-API.
